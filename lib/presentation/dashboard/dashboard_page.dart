import 'package:flutter/material.dart';

class DashboardPage extends StatelessWidget {
  const DashboardPage({Key? key}) : super(key: key);

  static final String TAG = '/Dsahboard';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
